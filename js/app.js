document.getElementById('year').textContent = new Date().getFullYear();
const form = document.getElementById('search-form'), message = document.getElementById('search-message'), result = document.getElementById('trip-result');
form.addEventListener('submit', async event => { event.preventDefault(); const button = form.querySelector('button'), query = document.getElementById('search-query').value.trim(); if (!query) return; button.disabled = true; button.textContent = 'Buscando…'; message.textContent = ''; result.hidden = true; try { const trip = await PepeGoApi.search(query); result.innerHTML = TripCard.render(trip); result.hidden = false; result.scrollIntoView({ behavior: 'smooth', block: 'start' }); } catch (error) { message.textContent = error.message; } finally { button.disabled = false; button.innerHTML = 'Buscar <span aria-hidden="true">→</span>'; } });
